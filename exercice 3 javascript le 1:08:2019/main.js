const navbar = document.querySelector('#navbar');


/*querySelector c'est pour sélectionner la navbar*/
/*window c pour clicker sur toute la page*/
/*scroll remplace le click,
après le 150Px commence le scroll*/

window.addEventListener('scroll', function() {
      if(si mon scoll est superieur à 150 px alors faire){
      	//ajouter une class CSS permettant de fixer ma navbar
      };
    });

/*Il faut que je trouve comment déclencher un évènement avec une const
et fixer la navbar avec js/*